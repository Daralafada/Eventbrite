<!DOCTYPE html>
<html>
  <head>
    <title>Eventbrite</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <div class="container-fluid">
  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <ul class="nav navbar-nav">
        <li><a id="len1" class="hoverable" href="#">Accueil</a></li>
        <li><a id="len2" class="hoverable" href="#">Créer un évènement</a></li>
        <li><a id="len3" class="hoverable" href="#">Profil</a></li>
        <li><a id="len4" class="hoverable" href="#">Contact</a>
        <% if current_user %>
                <a href="#" class="hoverable">Ton profil</a>
                <a href="#" class="hoverable">Se déconnecter</a>

              <%else %>
                <a href=new_user_registration class="hoverable">S'inscrire</a>
                <a href=new_user_session class="hoverable"> Se connecter</a> %>
              <%end%></li>
      </ul>
    </div>
  </nav>
  <div id="what-the-hell-is-this">
    <div class="page-title">
      <h2>Simple Navigation</h2>
      <p class="lead">
        Based on Hover.css, the goal of this pen
        is to create a simple navigation bar <br />
        that can be easily reused in both mobile and native displays. Mouse over the nav text for animation!
      </p>
    </div>
  </div>
</div>

  <body>
    <%= yield %>
  </body>
</html>
